transform:
  img_width: 480
  img_height: 480 

num_channels: 3
num_classes: 1   
num_levels: 4

accelerator: 'cuda'
devices: [0]

optimizer_type: 'sgd'  
learning_rate: 0.01    
loss_type: 'bce_with_logits_loss' 

min_epochs: 1 
max_epochs: 50
log_every_n_steps: 16

limit_train_batches: 0.9
limit_val_batches: 0.4
limit_test_batches: 0.25

train:
  root_dir: /home/charanarikala/cable_dataset/processed/train 
  batch_size: 8  
  shuffle: True

val:
  root_dir: /home/charanarikala/cable_dataset/processed/val
  batch_size: 4
  shuffle: False

test:
  root_dir: /home/charanarikala/cable_dataset/processed/test
  batch_size: 1
  shuffle: False

log_name: 'segnet_exp_01'
log_path: './experiments/exp03/logs' 
